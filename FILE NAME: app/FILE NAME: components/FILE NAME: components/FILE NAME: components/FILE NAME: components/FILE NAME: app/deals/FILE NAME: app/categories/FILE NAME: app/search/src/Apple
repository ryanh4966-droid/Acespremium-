export const metadata = {
  title: "AcesPremium",
  description: "Cinematic, intelligence‑driven shopping platform",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
